<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow http:// API calls (common for LAN + dev backends). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>

